---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/cert-manager.io/certificate_v1.json
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: admin
spec:
  secretName: admin-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: admin.reli.cc
  dnsNames:
    - admin.reli.cc
    - "*.admin.reli.cc"
