---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./oci-repository.yaml
  - ./helm-release.yaml

  - ./admin # -> local network, admin access
  - ./internal # -> local network, client access

